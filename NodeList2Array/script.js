const body = document.querySelector('body')
const divs = document.querySelectorAll('div');

// Traverse each div for subelements
// divs.forEach(div => {
//     const nxt = div.nextSibling;
//     console.log(nxt)
// })

const elems = getElementsByTagName('h2,h3,h4,h5');
console.log(elems)